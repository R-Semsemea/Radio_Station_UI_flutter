/*import 'package:flutter/material.dart';
import 'package:flutter_svg/svg.dart';
import 'package:podcast_app_xd/constants.dart';
import '../controller/tutorial_controller.dart';



///Review Getx




class Test extends StatelessWidget {
  List<Expanded> tutorialScreens = <Expanded>[
    Expanded(child: SvgPicture.string(picture)),
    Expanded(child: SvgPicture.string(picture)),
    Expanded(child: SvgPicture.string(picture)),
  ];

  @override
  build(BuildContext context) {
    return Scaffold(
      body: Stack(
        children: [
          background,
          Container(
            padding:
                EdgeInsets.symmetric(vertical: heightOfScreen(context, 0.07)),
            child: TabBarView(
              controller: tabController,
              children: [

              ],
            ),
          )
        ],
      ),
    );
  }
}
*/